import * as React from "react";
import Zoom from "./zoom";

export class Index extends React.Component<{}, {}> {
  render() {
    return (
      <div id="Zoom">
        <Zoom />
      </div>
    );
  }
}